/** Shared border/surface/padding (no overflow). */
export const bentoPanelChrome =
  "rounded-xl border border-border bg-card/40 p-4 shadow-sm"

/**
 * Scrollable bento tile — matches folder `SegmentViewNode` overview/files panels.
 */
export const bentoPanelClass = `${bentoPanelChrome} min-h-0 overflow-y-auto`
